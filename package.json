{
  "name": "action-format-build-name",
  "version": "1.0.0",
  "description": "GitHub action to format a PEW standardized artifact name",
  "main": "lib/main.js",
  "scripts": {
    "build": "tsc && ncc build"
  },
  "devDependencies": {
    "@types/node": "^20.5.2",
    "@vercel/ncc": "^0.36.0",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@actions/core": "^1.10.0"
  }
}
