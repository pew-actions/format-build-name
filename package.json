{
  "name": "action-format-build-name",
  "version": "1.0.0",
  "description": "GitHub action to format a PEW standardized artifact name",
  "main": "lib/main.js",
  "scripts": {
    "build": "tsc && ncc build"
  },
  "devDependencies": {
    "@types/node": "^18.8.5",
    "@zeit/ncc": "^0.22.3",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@actions/core": "^1.10.0"
  }
}
